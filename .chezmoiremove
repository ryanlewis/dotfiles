# Files chezmoi should remove from the home directory on apply.
# Migrated from .tool-versions to ~/.config/mise/config.toml (April 2026)
.tool-versions

# Removed agent no longer in use (April 2026)
.claude/agents/cursor-code-orchestrator.md

# Removed unprovisioned tooling (June 2026): yazi `y` helper and the wtfutil
# dashboard config (neither tool is installed by any managed source).
.config/zsh/functions/y.zsh
.config/wtf/config.yml

# Fish shell retired (June 2026): zsh is now the only managed shell. Remove the
# whole managed fish config tree from existing installs.
.config/fish

# tmux decommissioned in favour of herdr (June 2026): config, helper scripts,
# the tpm clone, and the tmux-based shell functions. (Fish helpers are covered
# by the .config/fish removal above.)
.tmux.conf
.tmux
.config/tmux
.config/zsh/functions/ta.zsh
.config/zsh/functions/ca.zsh
.config/zsh/functions/dogpound.zsh
.config/zsh/functions/tmux-keys.zsh
.claude/commands/branch-to-tmux-split.md

# zsh-abbr removed (June 2026): replaced by a tiny built-in ZLE abbreviation
# widget (ZABBR in .zshrc) — it didn't expand reliably in our plugin stack,
# errored on every startup, and cost ~16ms. Drop the cloned plugin tree.
.config/zsh/plugins/zsh-abbr

# NOTE: orphaned repo-meta files (Dockerfile, install.sh, etc.) cannot be
# purged here — they are listed in .chezmoiignore, and chezmoi never removes
# ignored targets. They are cleaned up by run_once_before_purge-orphaned-
# repo-meta.sh instead.
